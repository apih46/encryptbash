Encrypt sh file

```
wget -q -O /usr/bin/enc "https://raw.githubusercontent.com/apih46/encryptbash/main/enc.sh" && chmod +x /usr/bin/enc && apt install make && apt install gcc && apt install shc && enc
```
